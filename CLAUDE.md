# Project conventions

## Git commits

- Write commit messages the way a person would write them: a short, plain-language summary of what changed and roughly why. Not a bullet-pointed technical breakdown of every internal detail, root cause, and file touched.
- Don't append a `Co-Authored-By: Claude ...` trailer to every commit. Use it sparingly — only every now and then, not as a routine default.
